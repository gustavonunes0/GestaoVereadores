# Specs

This folder contains feature specifications.

Each bounded context must have its own folder.

Example:

```txt
docs/specs/
├── courses/
│   └── SPEC-001-courses-crud.md
├── enrollments/
└── certificates/
```

Specs define what must be built.

The `.cursorrules` file defines how the project should be organized.
